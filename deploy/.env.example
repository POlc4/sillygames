# Copier en /opt/sillygames/.env sur la VM. Jamais commité.
DOMAIN=sillygames.duckdns.org
GHCR_OWNER=polc4
# Tag d'image déployé ; le workflow de déploiement le remplace par sha-<commit>.
IMAGE_TAG=latest
# openssl rand -hex 24
POSTGRES_PASSWORD=
# openssl rand -hex 32
JWT_SECRET=
